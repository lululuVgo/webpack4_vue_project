/* vuex 状态管理 */
import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex);

const state = {

};

const mutations = {
  test(state,value){

  }
};

const actions = {};

export default new Vuex.Store({
  state,
  actions,
  mutations
})
